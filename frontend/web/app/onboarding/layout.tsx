import { ReactNode } from "react";

export const metadata = {
  title: "Onboarding | Ritual Ascension",
  description: "Connect your wallet, choose a builder class, and mint your soulbound passport."
};

export default function OnboardingLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
