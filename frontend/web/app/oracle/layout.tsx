import { ReactNode } from "react";

export const metadata = {
  title: "Oracle | Ritual Ascension",
  description: "Ask the Ritual Ascension Oracle Mentor for context-aware quest guidance."
};

export default function OracleLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
